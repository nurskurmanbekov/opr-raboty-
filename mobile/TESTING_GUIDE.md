# 📱 Руководство по тестированию мобильного приложения

## 🎯 Рекомендуемый способ - Development Build (APK)

Так как приложение использует фоновое отслеживание GPS (TaskManager), нужно собрать полноценный APK.

---

## 📋 Подготовка

### 1. Настройте API URL

Вам нужно узнать IP адрес вашего компьютера в локальной сети WiFi:

**Windows:**
```bash
ipconfig
```
Найдите "IPv4 Address" в разделе WiFi адаптера (например: `192.168.1.5`)

**Linux:**
```bash
hostname -I
# или
ip addr show
```

**Создайте файл `.env` в папке `mobile/`:**
```bash
cd mobile
cp .env.example .env
```

**Отредактируйте `.env` файл:**
```
EXPO_PUBLIC_API_URL=http://192.168.1.5:5000/api
```
*Замените `192.168.1.5` на ваш реальный IP адрес!*

---

## 🏗️ Способ 1: Локальная сборка APK (без облака)

### Установите EAS CLI:
```bash
npm install -g eas-cli
```

### Создайте конфигурацию EAS:
```bash
cd mobile
eas build:configure
```

### Соберите APK локально:
```bash
eas build --profile development --platform android --local
```

**Примечание:** Для локальной сборки нужен установленный Android SDK

**Результат:** APK файл будет создан в текущей папке

---

## ☁️ Способ 2: Облачная сборка через EAS (Рекомендуется!)

### 1. Зарегистрируйтесь на Expo:
```bash
cd mobile
eas login
# или
eas register
```

### 2. Настройте проект:
```bash
eas build:configure
```

### 3. Соберите APK в облаке:
```bash
eas build --profile preview --platform android
```

### 4. Скачайте APK:
После завершения сборки (5-15 минут) вы получите ссылку для скачивания APK файла.
Можете скачать прямо на телефон или на компьютер.

---

## 📲 Установка APK на Android

### Вариант A: Через USB кабель
1. Скачайте APK на компьютер
2. Подключите телефон к компьютеру через USB
3. Скопируйте APK на телефон
4. На телефоне откройте APK файл
5. Разрешите установку из неизвестных источников (если спросит)
6. Установите приложение

### Вариант B: Через WiFi (QR код)
1. После сборки EAS покажет QR код
2. Отсканируйте QR код на телефоне
3. Скачайте APK прямо на телефон
4. Установите

---

## 🚀 Запуск бэкенда для тестирования

### 1. Запустите сервер на компьютере:
```bash
cd backend
npm start
```

**Важно:** Сервер должен слушать на `0.0.0.0`, а не на `localhost`!

Проверьте в `backend/server.js`:
```javascript
const PORT = process.env.PORT || 5000;
app.listen(PORT, '0.0.0.0', () => {
  console.log(`Server running on http://0.0.0.0:${PORT}`);
});
```

### 2. Проверьте доступность API с телефона:

**На компьютере узнайте IP:**
```bash
# Windows
ipconfig

# Linux
hostname -I
```

**На телефоне откройте браузер:**
```
http://192.168.1.5:5000/api/auth/health
```
*Замените IP на ваш!*

Должны увидеть ответ от сервера (или страницу с информацией о API)

---

## 🔧 Профили сборки (eas.json)

Мы создали 3 профиля:

### **development** - для разработки
- Включает dev tools
- Можно подключить отладчик
- Больший размер APK

### **preview** - для тестирования
- Оптимизированная сборка
- Без dev tools
- Меньший размер
- **Рекомендуется для вашего случая!**

### **production** - для публикации
- Полная оптимизация
- Требует ключ подписи
- Для Google Play Store

---

## 🐛 Отладка

### Просмотр логов на Android:
```bash
# Подключите телефон через USB с включенной отладкой
adb logcat | grep ReactNativeJS
```

### Проверка сетевого подключения:
1. Убедитесь что компьютер и телефон в одной WiFi сети
2. Отключите файрвол на компьютере (временно)
3. Проверьте что порт 5000 открыт:
```bash
# Linux
sudo ufw allow 5000

# Windows
# Добавьте правило в Windows Firewall для порта 5000
```

---

## 📝 Тестовые данные

После установки APK и запуска бэкенда, войдите с тестовыми данными:

```
Email: client1@probation.kg
Пароль: 123456
```

---

## ❓ Частые проблемы

### "Network request failed"
- Проверьте IP адрес в `.env`
- Убедитесь что бэкенд запущен
- Проверьте что телефон и компьютер в одной сети
- Отключите VPN на телефоне

### "Cannot connect to server"
- Проверьте файрвол на компьютере
- Убедитесь что сервер слушает `0.0.0.0`, а не `localhost`
- Пингуйте компьютер с телефона

### "Location permission denied"
- Зайдите в настройки Android
- Приложения → Пробация КР → Разрешения
- Разрешите "Местоположение" → "Всегда"

---

## 🎉 Готово!

После установки APK вы сможете:
- ✅ Тестировать все функции приложения
- ✅ Фоновое отслеживание GPS работает
- ✅ Камера и галерея работают
- ✅ Геозоны работают
- ✅ Оффлайн режим работает

Удачного тестирования! 🚀

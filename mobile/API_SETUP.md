# 🔧 Настройка API для Mobile приложения

## Проблема: Network Error

Если вы видите ошибки `Network Error` в Mobile приложении, значит приложение не может подключиться к Backend.

## ✅ Решение

### Шаг 1: Узнайте IP адрес вашего компьютера

**Windows:**
```bash
ipconfig
```
Найдите **IPv4 Address**, например: `10.99.7.91` или `192.168.1.5`

**Mac/Linux:**
```bash
ifconfig | grep "inet "
```

### Шаг 2: Настройте API URL

**Вариант A - Через .env файл (РЕКОМЕНДУЕТСЯ):**

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте файл `.env` и замените `YOUR_IP_ADDRESS` на ваш IP:
   ```
   EXPO_PUBLIC_API_URL=http://10.99.7.91:5000/api
   ```

**Вариант B - Прямо в коде:**

Откройте `mobile/src/api/axios.js` и измените строку 23:
```javascript
const LOCAL_IP = '10.99.7.91'; // <-- Ваш IP адрес
```

### Шаг 3: Перезапустите приложение

1. В терминале где запущен Expo нажмите `r` для reload
2. Или остановите (`Ctrl+C`) и запустите снова:
   ```bash
   npm start
   ```

## 🔍 Проверка

После запуска вы должны увидеть в консоли:
```
📡 Mobile API URL: http://10.99.7.91:5000/api
```

## ⚠️ Важно

- ✅ Backend должен быть запущен: `node server.js`
- ✅ Компьютер и телефон должны быть в **одной WiFi сети**
- ✅ Backend слушает на `0.0.0.0:5000` (доступен из сети)
- ❌ НЕ используйте `localhost` или `127.0.0.1`

## 🆘 Если не работает

### Проверьте Backend:
```bash
cd backend
node server.js
```

Должно быть:
```
🚀 Server running on port 5000
🌐 Local: http://localhost:5000
📱 Network: Server listening on all network interfaces (0.0.0.0:5000)
```

### Проверьте подключение:

**С телефона откройте браузер и зайдите на:**
```
http://10.99.7.91:5000/api/health
```

Если видите JSON ответ - подключение работает! ✅

### Отключите Firewall (временно для теста):

**Windows:**
- Поиск → "Firewall" → Разрешить приложение через брандмауэр
- Найдите Node.js и разрешите для частных сетей

## 📱 Android Эмулятор

Для Android эмулятора используется специальный адрес `10.0.2.2`:
- Код автоматически определяет эмулятор
- Не нужно менять настройки

## 🎯 Тестовые аккаунты

После настройки API:
- Email: `client1@probation.kg`
- Пароль: `123456`

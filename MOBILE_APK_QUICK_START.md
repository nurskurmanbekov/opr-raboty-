# 🚀 Быстрый старт - Тестирование мобильного приложения

## 📱 Пошаговая инструкция

### Шаг 1: Узнайте IP адрес компьютера

**Windows:**
```bash
ipconfig
```
Найдите `IPv4 Address` (например: `192.168.1.5`)

**Linux:**
```bash
hostname -I
# Первый IP адрес - это ваш локальный IP
```

### Шаг 2: Настройте мобильное приложение

```bash
cd mobile
cp .env.example .env
```

Отредактируйте файл `.env`:
```
EXPO_PUBLIC_API_URL=http://192.168.1.5:5000/api
```
**❗ Замените `192.168.1.5` на ваш реальный IP!**

### Шаг 3: Запустите бэкенд

```bash
cd backend
npm start
```

Должны увидеть:
```
🚀 Server running on port 5000
📱 Network: Server listening on all network interfaces (0.0.0.0:5000)
```

### Шаг 4: Соберите APK

**Установите EAS CLI (один раз):**
```bash
npm install -g eas-cli
```

**Войдите в Expo:**
```bash
cd mobile
eas login
# Если нет аккаунта: eas register
```

**Соберите APK:**
```bash
eas build --profile preview --platform android
```

⏱️ Сборка займет **5-15 минут**

После завершения вы получите:
- ✅ QR код для скачивания APK на телефон
- ✅ Прямую ссылку на скачивание

### Шаг 5: Установите APK на телефон

**Вариант A - QR код:**
1. Отсканируйте QR код камерой телефона
2. Откройте ссылку
3. Скачайте APK
4. Разрешите установку
5. Установите приложение

**Вариант B - Прямая ссылка:**
1. Скопируйте ссылку на APK
2. Откройте на телефоне в браузере
3. Скачайте и установите

### Шаг 6: Тестируйте!

**Убедитесь что:**
- ✅ Телефон и компьютер в одной WiFi сети
- ✅ Бэкенд запущен на компьютере
- ✅ IP адрес в `.env` правильный

**Войдите в приложение:**
```
Email: client1@probation.kg
Пароль: 123456
```

---

## 🔥 Альтернатива: Expo Go (быстро, но с ограничениями)

Если хотите быстро посмотреть UI (без фоновых задач):

```bash
cd mobile
npm start
# или
npx expo start
```

1. Установите **Expo Go** из Google Play
2. Отсканируйте QR код в терминале
3. Приложение откроется в Expo Go

**⚠️ Ограничения Expo Go:**
- ❌ Фоновое отслеживание GPS не работает
- ❌ Некоторые нативные модули недоступны
- ✅ Можно посмотреть UI/UX
- ✅ Тестировать основные функции

---

## 🐛 Решение проблем

### "Network request failed"
1. Проверьте IP в `.env` - должен быть IP компьютера, не `localhost`
2. Убедитесь что бэкенд запущен
3. Откройте в браузере телефона: `http://ВАШ_IP:5000`

### Не могу подключиться к серверу
```bash
# Linux - откройте порт
sudo ufw allow 5000

# Windows - добавьте правило в Firewall:
# Панель управления → Брандмауэр Windows → Дополнительные параметры
# → Правила для входящих подключений → Создать правило
# Порт: 5000, TCP, Разрешить подключение
```

### APK не устанавливается
- Разрешите установку из неизвестных источников
- Настройки → Безопасность → Неизвестные источники

---

## 📚 Полная документация

См. `mobile/TESTING_GUIDE.md` для подробной информации.

---

## ✅ Что работает в APK

✅ Авторизация клиентов
✅ Отображение статистики
✅ Начало/завершение рабочей сессии
✅ **Фоновое отслеживание GPS** (работает только в APK!)
✅ Загрузка фото (камера/галерея)
✅ Проверка геозон
✅ Уведомления
✅ Оффлайн режим и синхронизация
✅ Профиль пользователя

Удачного тестирования! 🎉

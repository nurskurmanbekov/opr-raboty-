# 🪟 УСТАНОВКА С НУЛЯ - WINDOWS

**Полная инструкция для запуска проекта на Windows ноутбуке**

Время: ~30 минут
Сложность: Легко (копируй и вставляй команды)

---

## ✅ ЧТО НУЖНО УСТАНОВИТЬ

```
1. Docker Desktop  (для запуска контейнеров)
2. Git             (для скачивания проекта)
3. Проект          (клонировать с GitHub)
```

---

## 📦 ШАГ 1: УСТАНОВКА DOCKER DESKTOP

### 1.1 Скачать Docker Desktop

Открой браузер и скачай Docker Desktop:

👉 **https://www.docker.com/products/docker-desktop/**

- Нажми **"Download for Windows"**
- Скачается файл: `Docker Desktop Installer.exe` (~500 MB)

### 1.2 Установить Docker Desktop

1. Запусти скачанный файл `Docker Desktop Installer.exe`
2. Установщик спросит: **"Use WSL 2 instead of Hyper-V"** → Оставь **галочку** ✅
3. Нажми **"OK"** и жди установки (~5 минут)
4. Нажми **"Close and restart"** (компьютер перезагрузится)

### 1.3 Первый запуск Docker Desktop

После перезагрузки:

1. Найди в меню "Пуск" → **Docker Desktop** → запусти
2. Дождись пока Docker запустится (~2 минуты)
3. Увидишь окно Docker Desktop с зеленой иконкой внизу: **"Engine running"** ✅

### 1.4 Проверка Docker

Открой **PowerShell** (Win + X → "Windows PowerShell"):

```powershell
docker --version
```

**Должно показать:**
```
Docker version 24.0.x, build xxxxx
```

✅ Отлично! Docker готов!

---

## 🔧 ШАГ 2: УСТАНОВКА GIT

### 2.1 Скачать Git

👉 **https://git-scm.com/download/win**

- Скачается файл: `Git-2.x.x-64-bit.exe` (~50 MB)

### 2.2 Установить Git

1. Запусти `Git-2.x.x-64-bit.exe`
2. Нажимай **"Next"** на всех экранах (оставляй настройки по умолчанию)
3. Дождись установки (~2 минуты)
4. Нажми **"Finish"**

### 2.3 Проверка Git

Открой **новое** окно PowerShell:

```powershell
git --version
```

**Должно показать:**
```
git version 2.x.x.windows.x
```

✅ Git установлен!

---

## 📥 ШАГ 3: СКАЧАТЬ ПРОЕКТ

### 3.1 Создать папку для проектов

Открой PowerShell и выполни:

```powershell
# Создать папку "Projects" на диске C:
cd C:\
mkdir Projects
cd Projects
```

### 3.2 Клонировать проект с GitHub

```powershell
# Скачать проект (замени на свой URL если другой)
git clone https://github.com/nurskurmanbekov/opr-raboty-.git

# Войти в папку проекта
cd opr-raboty-
```

**Что произойдет:**
- Git скачает все файлы проекта (~50 MB)
- Создаст папку `C:\Projects\opr-raboty-`

### 3.3 Проверить что скачалось

```powershell
# Посмотреть файлы
ls
```

**Должны увидеть:**
```
backend/
frontend/
mobile/
docker-compose.yml
docker-compose.dev.yml
docker-compose.full.yml
LAPTOP_TESTING_GUIDE.md
README.md
```

✅ Проект скачан!

---

## 🚀 ШАГ 4: ЗАПУСК ПРОЕКТА (ЛЕГКАЯ ВЕРСИЯ)

### 4.1 Убедись что Docker запущен

Посмотри в трей (правый нижний угол) → должна быть иконка Docker 🐳

Если Docker не запущен:
- Меню Пуск → Docker Desktop → запусти
- Дождись "Engine running" (зеленая иконка внизу)

### 4.2 Запустить проект

В PowerShell (находясь в папке `C:\Projects\opr-raboty-`):

```powershell
# Запустить легкую версию (без Face ID)
docker-compose -f docker-compose.dev.yml up -d
```

**Что произойдет:**
1. Docker скачает нужные образы (~1-2 GB) - **первый раз долго (10-15 минут)**
2. Запустит 3 контейнера: PostgreSQL, Backend, Frontend
3. Вернет управление в консоль

**Проверить статус:**

```powershell
docker-compose -f docker-compose.dev.yml ps
```

**Должно показать:**
```
NAME                    STATUS
probation_backend       Up
probation_frontend      Up
probation_postgres      Up (healthy)
```

### 4.3 Дождаться готовности (~2 минуты)

Смотреть логи backend:

```powershell
docker-compose -f docker-compose.dev.yml logs -f backend
```

**Дождись этой строки:**
```
✅ Server running on port 5000
✅ Database connected
```

Нажми **Ctrl+C** чтобы выйти из логов.

### 4.4 Открыть в браузере

Открой браузер (Chrome/Edge/Firefox):

```
Frontend:  http://localhost:5173
Backend:   http://localhost:5000/api
```

**Должен открыться интерфейс приложения!** 🎉

---

## 👤 ШАГ 5: СОЗДАТЬ SUPERADMIN

Чтобы войти в систему, нужен пользователь.

### 5.1 Войти в контейнер backend

```powershell
docker exec -it probation_backend sh
```

Теперь ты внутри контейнера! Увидишь:
```
/app #
```

### 5.2 Создать superadmin

```bash
node scripts/createSuperadmin.js
```

**Ответь на вопросы:**
```
Enter username: superadmin
Enter password: admin123
Enter full name: Администратор
```

**Увидишь:**
```
✅ Superadmin created successfully!
Username: superadmin
Password: admin123
```

### 5.3 Выйти из контейнера

```bash
exit
```

---

## 🎮 ШАГ 6: ТЕСТИРОВАНИЕ

### 6.1 Войти в систему

1. Открой: http://localhost:5173
2. Введи логин: `superadmin`
3. Введи пароль: `admin123`
4. Нажми **"Войти"**

**Ты в системе!** 🎉

### 6.2 Что можно делать:

- ✅ Создать районы
- ✅ Создать сотрудников
- ✅ Создать клиентов (без фото лица)
- ✅ Создать МТУ с геозонами
- ✅ Генерировать QR коды
- ✅ Смотреть статистику

**НЕ РАБОТАЕТ** в легкой версии:
- ❌ Загрузка фото лица при регистрации
- ❌ Face ID верификация

---

## 🛑 ОСТАНОВКА ПРОЕКТА

Когда закончишь тестировать:

```powershell
# Остановить контейнеры
docker-compose -f docker-compose.dev.yml down
```

**Запуск в следующий раз:**

```powershell
cd C:\Projects\opr-raboty-
docker-compose -f docker-compose.dev.yml up -d
```

---

## 🤖 БОНУС: ПОЛНАЯ ВЕРСИЯ (С FACE ID)

Если хочешь протестировать Face ID:

### Запуск

```powershell
# Остановить легкую версию
docker-compose -f docker-compose.dev.yml down

# Запустить полную версию
docker-compose -f docker-compose.full.yml up -d
```

**ВНИМАНИЕ:**
- Первый запуск: ~10-15 минут (скачивание образов)
- CompreFace стартует долго: ~5-10 минут
- Потребление RAM: ~6-8 GB

### Настройка CompreFace

1. Открой CompreFace Admin: http://localhost:8001
2. Создай аккаунт (любые данные)
3. Создай приложение: "Probation System"
4. Скопируй **API Key**
5. Создай файл `backend/.env` и добавь:
   ```
   COMPREFACE_API_KEY=твой_скопированный_ключ
   ```
6. Перезапусти backend:
   ```powershell
   docker-compose -f docker-compose.full.yml restart backend
   ```

Теперь Face ID работает! ✅

---

## 🔍 МОНИТОРИНГ РЕСУРСОВ

### Память контейнеров

```powershell
docker stats --no-stream
```

### Свободная RAM

```powershell
Get-CimInstance Win32_OperatingSystem | Select @{N="Free GB";E={[math]::Round($_.FreePhysicalMemory/1MB,2)}}
```

---

## ❓ ЧАСТЫЕ ПРОБЛЕМЫ

### "Docker is not running"

**Решение:**
```
Пуск → Docker Desktop → запустить
Дождаться зеленой иконки "Engine running"
```

### "port is already allocated"

**Решение:**
```powershell
# Найти что занимает порт 5000
netstat -ano | findstr :5000

# Убить процесс (замени PID на число из предыдущей команды)
taskkill /PID <номер> /F
```

### "Cannot connect to database"

**Решение:**
```powershell
# Перезапустить всё
docker-compose -f docker-compose.dev.yml down
docker-compose -f docker-compose.dev.yml up -d
```

### Docker Desktop очень медленный

**Решение:**
```
Docker Desktop → Settings → Resources
- CPUs: минимум 4
- Memory: минимум 4 GB (для dev.yml) или 8 GB (для full.yml)
- Нажми Apply & Restart
```

---

## 📊 СВОДКА КОМАНД

```powershell
# Скачать проект
git clone https://github.com/nurskurmanbekov/opr-raboty-.git
cd opr-raboty-

# Запустить легкую версию
docker-compose -f docker-compose.dev.yml up -d

# Создать superadmin
docker exec -it probation_backend sh
node scripts/createSuperadmin.js
exit

# Открыть в браузере
start http://localhost:5173

# Остановить
docker-compose -f docker-compose.dev.yml down
```

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] Docker Desktop установлен и запущен
- [ ] Git установлен
- [ ] Проект скачан в `C:\Projects\opr-raboty-`
- [ ] Запущен `docker-compose.dev.yml`
- [ ] Frontend открылся: http://localhost:5173
- [ ] Создан superadmin
- [ ] Успешный вход в систему

**Всё работает?** → Можно тестировать! 🚀

---

## 🎯 ЧТО ДАЛЬШЕ?

1. **Протестируй основной функционал** (30 минут):
   - Создание районов, сотрудников, клиентов
   - Создание МТУ с геозонами
   - Генерация QR кодов

2. **Попробуй полную версию** (если хватит времени):
   - Запусти `docker-compose.full.yml`
   - Настрой CompreFace
   - Протестируй Face ID

3. **Изучи код** (опционально):
   - Backend: `backend/`
   - Frontend: `frontend/src/`
   - Mobile: `mobile/src/`

---

**Удачи!** 🍀

Если что-то не получается - пиши, помогу! 😊

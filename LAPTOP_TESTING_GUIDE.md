# 💻 ИНСТРУКЦИЯ ДЛЯ ТЕСТИРОВАНИЯ НА НОУТБУКЕ

## 📊 ТВОЯ СИСТЕМА

```
RAM:      16 GB (2x8GB Samsung DDR4 3200MHz)
Свободно: ~9 GB
Docker:   Должен быть установлен
```

**ВЫВОД:** Памяти хватит для всего! 🎉

---

## 🚀 ВАРИАНТ 1: БЫСТРЫЙ СТАРТ (БЕЗ FACE ID)

**Требования:** ~2-3 GB RAM
**Время запуска:** 2-3 минуты
**Что работает:** ВСЁ кроме Face ID

### Шаг 1: Запуск

```bash
# Запустить базовые сервисы
docker-compose -f docker-compose.dev.yml up -d

# Проверить статус
docker-compose -f docker-compose.dev.yml ps
```

### Шаг 2: Дождаться готовности

```bash
# Логи backend (дождаться "Server running on port 5000")
docker-compose -f docker-compose.dev.yml logs -f backend

# Логи frontend (дождаться "Local: http://localhost:5173")
docker-compose -f docker-compose.dev.yml logs -f frontend
```

### Шаг 3: Открыть в браузере

```
Frontend:  http://localhost:5173
Backend:   http://localhost:5000/api
```

### Шаг 4: Создать superadmin

```bash
# Войти в контейнер backend
docker exec -it probation_backend sh

# Создать суперадмина
node scripts/createSuperadmin.js

# Выйти
exit
```

### Шаг 5: Тестировать!

Логин: `superadmin` / пароль из вывода команды

**Что можно тестировать:**
- ✅ Создание районов
- ✅ Создание сотрудников
- ✅ Создание клиентов (без фото лица)
- ✅ Создание МТУ с геозонами
- ✅ QR коды для МТУ
- ✅ Рабочие сессии
- ✅ GPS трекинг
- ✅ Фотоотчеты
- ✅ Статистику

**Не работает:**
- ❌ Загрузка фото лица при регистрации клиента
- ❌ Face ID верификация

### Остановка

```bash
docker-compose -f docker-compose.dev.yml down
```

---

## 🤖 ВАРИАНТ 2: ПОЛНЫЙ ЗАПУСК (С FACE ID)

**Требования:** ~6-8 GB RAM (ОПТИМИЗИРОВАНО!)
**Время запуска:** 5-10 минут (CompreFace долго стартует)
**Что работает:** ВСЁ включая Face ID

### Шаг 1: Запуск

```bash
# Запустить все сервисы (оптимизированные)
docker-compose -f docker-compose.full.yml up -d

# Проверить статус
docker-compose -f docker-compose.full.yml ps
```

### Шаг 2: Мониторинг памяти

```powershell
# Windows PowerShell - следить за памятью
while ($true) {
    $mem = Get-CimInstance Win32_OperatingSystem
    $free = [math]::Round($mem.FreePhysicalMemory/1MB,2)
    Write-Host "Свободно: $free GB" -ForegroundColor Green
    Start-Sleep -Seconds 5
}
```

### Шаг 3: Дождаться CompreFace (~5 минут)

```bash
# Проверить статус CompreFace API
docker-compose -f docker-compose.full.yml logs -f compreface_api

# Должно появиться: "Started EmbeddedRecognitionApplication"
```

### Шаг 4: Настроить CompreFace

1. Открыть Admin: http://localhost:8001
2. Создать аккаунт (любые данные)
3. Создать приложение: "Probation System"
4. Скопировать API Key
5. Добавить в backend/.env:
   ```
   COMPREFACE_API_KEY=скопированный_ключ
   ```
6. Перезапустить backend:
   ```bash
   docker-compose -f docker-compose.full.yml restart backend
   ```

### Шаг 5: Тестировать Face ID!

Теперь при создании клиента можно загружать фото лица (3-5 фото).

**Проверка:**
```bash
# Проверить что backend видит CompreFace
docker-compose -f docker-compose.full.yml logs backend | grep CompreFace
```

### Остановка

```bash
docker-compose -f docker-compose.full.yml down
```

---

## 🔍 СРАВНЕНИЕ ВЕРСИЙ

| Параметр | docker-compose.dev.yml | docker-compose.full.yml |
|----------|------------------------|-------------------------|
| **RAM** | ~2-3 GB | ~6-8 GB |
| **Старт** | 2-3 мин | 5-10 мин |
| **Face ID** | ❌ | ✅ |
| **Все остальное** | ✅ | ✅ |

---

## 💡 ОПТИМИЗАЦИИ В FULL ВЕРСИИ

### Было (по умолчанию):
```yaml
compreface_admin: -Xmx2g     # 2 GB
compreface_api:   -Xmx2g     # 2 GB
compreface_core:  no limit   # 2-4 GB
ИТОГО: ~10 GB
```

### Стало (оптимизировано):
```yaml
compreface_admin: -Xmx512m   # 512 MB
compreface_api:   -Xmx1g     # 1 GB
compreface_core:  limit 3G   # 3 GB max
ИТОГО: ~6 GB
```

**Экономия: ~40% памяти!** 🎉

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. CompreFace долго стартует

**Решение:** Подожди 5-10 минут, это нормально для ML сервисов.

### 2. "Out of Memory" ошибка

**Решение:** Закрой браузер Chrome (он жрет много RAM) или используй `docker-compose.dev.yml`

### 3. Backend не подключается к CompreFace

**Решение:**
```bash
# Проверить что CompreFace API запущен
curl http://localhost:8002/api/actuator/health

# Должен вернуть: {"status":"UP"}
```

### 4. Медленная работа Face ID

**Решение:** Это нормально для оптимизированной версии. Для production нужен мощнее сервер.

---

## 📊 МОНИТОРИНГ РЕСУРСОВ

### Windows PowerShell

```powershell
# Память Docker контейнеров
docker stats --no-stream --format "table {{.Container}}\t{{.MemUsage}}"

# Свободная RAM
Get-CimInstance Win32_OperatingSystem | Select @{N="Free GB";E={[math]::Round($_.FreePhysicalMemory/1MB,2)}}
```

### Linux/Mac

```bash
# Память Docker контейнеров
docker stats --no-stream

# Свободная RAM
free -h
```

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

### Для docker-compose.dev.yml:
- [ ] Docker установлен
- [ ] Docker запущен
- [ ] Свободно минимум 3 GB RAM
- [ ] Порты 5000, 5173, 5432 свободны

### Для docker-compose.full.yml:
- [ ] Docker установлен
- [ ] Docker запущен
- [ ] Свободно минимум 6 GB RAM
- [ ] Порты 5000, 5173, 5432, 8001, 8002, 8003 свободны
- [ ] Готов подождать 5-10 минут для старта

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ ДЛЯ ТЕБЯ

**С твоими 16 GB RAM:**

1. **Сначала:** Запусти `docker-compose.dev.yml`
   - Проверь что всё работает (создание клиентов, МТУ, сессии)
   - Время: 30 минут тестирования

2. **Потом:** Запусти `docker-compose.full.yml`
   - Протестируй Face ID
   - Загрузи 3-5 фото
   - Попробуй верификацию

3. **Результат:** Поймешь работает ли система полностью!

---

**Готов попробовать?** 🚀

Рекомендую начать с **docker-compose.dev.yml** - быстро запустится и можно всё потестировать!

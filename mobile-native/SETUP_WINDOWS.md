# Установка React Native приложения на Windows

## Шаг 1: Проверить установленное ПО

Откройте командную строку (cmd) и выполните:

```cmd
node --version
npm --version
java -version
```

Если команды не найдены, нужно установить:

### 1. Node.js (обязательно)
- Скачать: https://nodejs.org/
- Установить версию LTS (Long Term Support)
- После установки перезапустить командную строку

### 2. Java JDK 17 (обязательно для Android)
- Скачать: https://adoptium.net/
- Установить JDK 17
- Добавить в PATH переменную JAVA_HOME

### 3. Android Studio (обязательно для сборки APK)
- Скачать: https://developer.android.com/studio
- Установить Android Studio
- Установить Android SDK (API 33+)
- Добавить в PATH переменные:
  - ANDROID_HOME = C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Local\Android\Sdk
  - Добавить в Path: %ANDROID_HOME%\platform-tools
  - Добавить в Path: %ANDROID_HOME%\tools

## Шаг 2: Установить зависимости проекта

```cmd
cd C:\probation_system\mobile-native
npm install
```

Это займет 5-10 минут. Будет установлено ~500 пакетов.

## Шаг 3: Настроить Google Maps API Key

1. Открыть файл: `android\app\src\main\AndroidManifest.xml`
2. Найти строку:
   ```xml
   android:value="YOUR_GOOGLE_MAPS_API_KEY_HERE"
   ```
3. Заменить на реальный API ключ (получить на https://console.cloud.google.com/)

**Или временно оставить как есть** - приложение будет работать, просто карта не загрузится.

## Шаг 4: Запуск в режиме разработки (опционально)

### 4a. Подключить Android устройство или запустить эмулятор

**Вариант А: Реальное устройство**
1. Включить "Режим разработчика" на телефоне (Settings → About → Tap Build Number 7 times)
2. Включить "USB Debugging" (Settings → Developer Options → USB Debugging)
3. Подключить телефон к компьютеру через USB
4. Проверить: `adb devices` (должно показать устройство)

**Вариант Б: Эмулятор Android Studio**
1. Открыть Android Studio
2. Tools → Device Manager
3. Create Virtual Device
4. Выбрать Pixel 5 или другое устройство
5. Скачать System Image (API 33 или выше)
6. Запустить эмулятор

### 4b. Запустить приложение

```cmd
cd C:\probation_system\mobile-native
npm start
```

В новом окне командной строки:

```cmd
cd C:\probation_system\mobile-native
npm run android
```

Приложение установится на устройство/эмулятор и запустится.

## Шаг 5: Сборка Release APK (для установки на телефон)

### 5a. Создать ключ подписи (только первый раз)

```cmd
cd C:\probation_system\mobile-native\android\app
keytool -genkeypair -v -storetype PKCS12 -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
```

Вам будут заданы вопросы:
- Enter keystore password: **придумайте пароль** (запомните его!)
- Re-enter password: **повторите пароль**
- What is your first and last name: **ваше имя**
- What is your organization: **название организации**
- И т.д. (можно нажимать Enter для значений по умолчанию)

### 5b. Настроить gradle.properties

Создать файл `C:\probation_system\mobile-native\android\gradle.properties`:

```properties
MYAPP_RELEASE_STORE_FILE=my-release-key.keystore
MYAPP_RELEASE_KEY_ALIAS=my-key-alias
MYAPP_RELEASE_STORE_PASSWORD=ваш_пароль_здесь
MYAPP_RELEASE_KEY_PASSWORD=ваш_пароль_здесь
```

### 5c. Собрать APK

```cmd
cd C:\probation_system\mobile-native\android
gradlew assembleRelease
```

Ждите 5-10 минут. После завершения APK будут в:

```
android\app\build\outputs\apk\release\app-arm64-v8a-release.apk  (основной)
android\app\build\outputs\apk\release\app-armeabi-v7a-release.apk
```

### 5d. Установить APK на телефон

**Способ 1: Через USB**
```cmd
adb install android\app\build\outputs\apk\release\app-arm64-v8a-release.apk
```

**Способ 2: Скопировать файл**
1. Скопировать `app-arm64-v8a-release.apk` на телефон
2. Открыть файл на телефоне
3. Разрешить установку из неизвестных источников
4. Установить

## ⚠️ Возможные проблемы

### "SDK location not found"
Создать файл `android\local.properties`:
```properties
sdk.dir=C:\\Users\\ВАШ_ПОЛЬЗОВАТЕЛЬ\\AppData\\Local\\Android\\Sdk
```

### "Execution failed for task ':app:validateSigningRelease'"
Проверить что файл `gradle.properties` создан и пароли правильные.

### "Unable to locate Android SDK"
Установить Android Studio и настроить ANDROID_HOME переменную окружения.

### NPM install ошибки
```cmd
npm cache clean --force
rm -rf node_modules
npm install
```

## 📱 Тестирование приложения

1. Открыть приложение на телефоне
2. Войти с тестовыми данными (которые есть в вашей базе данных)
3. Нажать "Начать смену"
4. Проверить что GPS работает
5. Проверить офлайн режим (включить Airplane Mode)

## 🎯 Готово!

Приложение готово к использованию. Размер APK будет около 12-15 MB.

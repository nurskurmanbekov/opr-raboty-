# 🌐 Настройка для работы в локальной сети

Эта инструкция покажет как дать доступ к приложению другим компьютерам в вашей локальной сети (офис, дом).

---

## 🎯 Что вы получите

После настройки:
- ✅ Вы можете работать: `http://localhost:5173`
- ✅ Коллеги могут работать: `http://ваш-IP:5173`
- ✅ Все видят одни и те же данные
- ✅ Работает в локальной сети без интернета

---

## 📋 Быстрая настройка (5 минут)

### **Шаг 1: Узнайте IP адрес вашего компьютера**

**Windows (cmd или PowerShell):**
```bash
ipconfig
```

Найдите строку **IPv4 Address**:
```
Адаптер беспроводной локальной сети (WiFi):
   IPv4-адрес. . . . . . . . . . . : 192.168.1.5
```

Или:
```
Адаптер Ethernet:
   IPv4-адрес. . . . . . . . . . . : 10.99.7.91
```

**Запомните этот IP!** (например: `10.99.7.91`)

---

### **Шаг 2: Настройте Frontend**

**Скопируйте файл настроек:**
```bash
cd frontend
copy .env.example .env
```

**Откройте** `frontend/.env` в блокноте и измените:

```env
# Было:
VITE_API_URL=http://localhost:5000/api

# Стало (замените IP на ваш!):
VITE_API_URL=http://10.99.7.91:5000/api
```

**Сохраните файл.**

---

### **Шаг 3: Откройте порты в Windows Firewall**

**PowerShell от администратора** (правой кнопкой → Запуск от имени администратора):

```powershell
# Порт 5000 - Backend
New-NetFirewallRule -DisplayName "Probation Backend" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow

# Порт 5173 - Frontend
New-NetFirewallRule -DisplayName "Probation Frontend" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow
```

Должно показать:
```
Name                  : {GUID}
DisplayName           : Probation Backend
Enabled               : True
...
```

---

### **Шаг 4: Запустите приложение**

**Терминал 1 - Backend:**
```bash
cd backend
npm start
```

Должны увидеть:
```
✅ Database connected successfully
🚀 Server running on port 5000
📱 Network: Server listening on all network interfaces (0.0.0.0:5000)
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Должны увидеть:
```
VITE v5.x.x  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: http://10.99.7.91:5173/    ← Дайте эту ссылку коллегам!
```

---

### **Шаг 5: Проверьте доступ**

**На вашем компьютере:**
```
http://localhost:5173
```

**На других компьютерах в сети:**
```
http://10.99.7.91:5173
```
(замените IP на ваш!)

**Должно работать!** 🎉

---

## 🔧 Дополнительные настройки

### **Если IP адрес меняется**

Если ваш компьютер каждый раз получает новый IP (DHCP):

**Вариант 1: Настроить статический IP в роутере**
- Зайдите в настройки роутера
- Найдите DHCP → Static IP
- Привяжите MAC адрес вашего ПК к фиксированному IP

**Вариант 2: Использовать имя компьютера**

В `frontend/.env`:
```env
VITE_API_URL=http://ИМЯ-КОМПЬЮТЕРА:5000/api
```

Узнать имя:
```bash
hostname
```

---

### **Если есть несколько сетевых адаптеров**

У вас может быть:
- WiFi адаптер: `192.168.1.5`
- Ethernet: `10.99.7.91`
- VPN: `172.16.0.5`

**Используйте IP того адаптера, к которому подключены коллеги!**

Обычно это WiFi или Ethernet.

---

### **Для удобства - локальный домен**

**Файл hosts** (нужны права администратора):

Windows: `C:\Windows\System32\drivers\etc\hosts`

Добавьте строку:
```
10.99.7.91  probation.local
```

Теперь можно использовать:
```
http://probation.local:5173
```

---

## 🔒 Безопасность

### **Где безопасно использовать:**
- ✅ Домашняя WiFi сеть
- ✅ Корпоративная сеть (офис)
- ✅ Приватная LAN

### **Где НЕ безопасно:**
- ❌ Публичный WiFi (кафе, аэропорт)
- ❌ Открытые сети
- ❌ Интернет (без SSL)

### **Защита:**
- ✅ Firewall блокирует доступ снаружи
- ✅ Аутентификация в приложении
- ✅ Роутер изолирует локальную сеть

---

## 🌍 Доступ через интернет (опционально)

Если нужно дать доступ людям **вне локальной сети**:

### **Вариант 1: Ngrok (для демо)**

```bash
# Установите: https://ngrok.com/download
ngrok http 5173
```

Получите публичный URL:
```
https://abc123.ngrok.io → ваше приложение
```

**Минусы:** временный URL, ограничения бесплатной версии

### **Вариант 2: VPS хостинг (для продакшена)**

Разместите на:
- DigitalOcean
- AWS EC2
- Heroku
- Vercel (frontend)
- Railway (backend)

---

## 📊 Производительность

### **Сколько пользователей поддерживается:**

**Локальная сеть (ваш ПК как сервер):**
- 5-10 пользователей: ✅ Отлично
- 10-20 пользователей: ✅ Хорошо
- 20-50 пользователей: ⚠️ Зависит от вашего ПК
- 50+ пользователей: ❌ Нужен выделенный сервер

**Зависит от:**
- Процессор вашего ПК
- Оперативная память
- Скорость сети
- База данных PostgreSQL

---

## ❓ Устранение проблем

### **Проблема 1: "This site can't be reached"**

**Решения:**
1. Проверьте Firewall - порты 5000 и 5173 открыты?
2. Пингуйте с другого ПК:
   ```bash
   ping 10.99.7.91
   ```
   Должен отвечать!
3. Убедитесь что оба ПК в одной сети

### **Проблема 2: Страница открывается, но "Network Error"**

**Решения:**
1. Проверьте что Backend запущен
2. Проверьте IP в `frontend/.env`
3. Откройте в браузере:
   ```
   http://10.99.7.91:5000
   ```
   Должен показать:
   ```json
   { "message": "🚀 Probation System API" }
   ```

### **Проблема 3: Frontend не показывает Network URL**

**Решение:**

`vite.config.js` должен содержать:
```javascript
server: {
  host: true
}
```

Это уже настроено! ✅

### **Проблема 4: "CORS error"**

Backend уже настроен на `cors: { origin: '*' }` - должно работать.

Если нет, проверьте `backend/server.js`:
```javascript
app.use(cors({
  origin: '*',
  credentials: true
}));
```

---

## 🎯 Итоговый чеклист

- [ ] Узнал свой IP адрес
- [ ] Скопировал `.env.example` в `.env`
- [ ] Указал свой IP в `frontend/.env`
- [ ] Открыл порты 5000 и 5173 в Firewall
- [ ] Запустил Backend (`npm start`)
- [ ] Запустил Frontend (`npm run dev`)
- [ ] Вижу Network URL в консоли Vite
- [ ] Проверил доступ с другого ПК
- [ ] Работает! 🎉

---

## 📞 Полезные команды

**Проверить какой порт слушает Backend:**
```bash
netstat -an | findstr :5000
```

**Проверить открытые порты в Firewall:**
```powershell
Get-NetFirewallRule -DisplayName "Probation*"
```

**Проверить доступность с другого ПК:**
```bash
# На другом ПК
curl http://10.99.7.91:5000
```

**Посмотреть все сетевые интерфейсы:**
```bash
ipconfig /all
```

---

## ✅ Готово!

Теперь ваше приложение доступно для всех компьютеров в локальной сети!

Если возникли проблемы - проверьте раздел "Устранение проблем" выше.

Удачной работы! 🚀

# 🚀 Система мониторинга общественных работ - Департамент пробации КР

Полнофункциональная система для управления и мониторинга общественных работ клиентов пробации Кыргызской Республики.

## 📋 Компоненты системы

- **Backend** - Node.js + Express + PostgreSQL
- **Frontend** - React + Vite + Tailwind CSS (веб-панель для сотрудников)
- **Mobile** - React Native + Expo (приложение для клиентов)

---

## 🛠️ Быстрый старт

### Предварительные требования

- Node.js 18+
- PostgreSQL 14+
- npm или yarn
- Expo Go (для мобильного приложения)

---

## 📦 Установка и запуск

### 1️⃣ Backend (API Server)

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
npm install

# Настроить базу данных
# Создайте БД в PostgreSQL:
createdb probation_db

# Или через psql:
psql -U postgres
CREATE DATABASE probation_db;
\q

# Настроить .env (уже создан, проверьте настройки)
# Отредактируйте backend/.env если нужно:
# - DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
# - JWT_SECRET

# Запустить сервер (он автоматически создаст таблицы)
npm start

# Или в режиме разработки с hot-reload:
npm run dev

# Создать тестовые данные
node seed.js
```

**Сервер запустится на:** `http://localhost:5000`

✅ **Тестовые данные созданы!**

---

### 2️⃣ Frontend (Веб-панель)

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

**Веб-панель доступна на:** `http://localhost:5173`

**Тестовые аккаунты для входа:**

- **Суперадмин:**
  - Email: `admin@probation.kg`
  - Пароль: `123456`

- **Администратор района:**
  - Email: `admin.bishkek@probation.kg`
  - Пароль: `123456`

- **Офицер/Куратор:**
  - Email: `officer@probation.kg`
  - Пароль: `123456`

---

### 3️⃣ Mobile (Мобильное приложение)

```bash
# Перейти в папку mobile
cd mobile

# Установить зависимости
npm install

# ВАЖНО: Настроить IP адрес!
# Откройте mobile/src/api/axios.js
# Замените '192.168.1.100' на IP вашего компьютера

# Найти свой IP:
# Windows: ipconfig
# Mac/Linux: ifconfig
# Ищите IPv4 адрес (например, 192.168.1.105)

# Запустить Expo
npm start

# Отсканируйте QR-код в приложении Expo Go
# Или нажмите:
# - 'a' для Android эмулятора
# - 'i' для iOS симулятора
```

**Тестовые аккаунты для клиентов:**

- Email: `client1@probation.kg` | Пароль: `123456`
- Email: `client2@probation.kg` | Пароль: `123456`
- Email: `client3@probation.kg` | Пароль: `123456`

---

## 🎯 Как протестировать систему

### Сценарий 1: Клиент начинает работу

1. **Откройте мобильное приложение**
2. **Войдите как клиент:** `client1@probation.kg` / `123456`
3. **На главном экране** увидите:
   - Прогресс выполнения часов
   - Статистику сессий
4. **Нажмите "Начать рабочую сессию"**
5. **Разрешите доступ к GPS**
6. **Сессия начнется**, таймер запустится
7. **Подождите немного**, затем нажмите **"Завершить сессию"**
8. **Часы автоматически подсчитаются** и сохранятся

### Сценарий 2: Офицер проверяет работу

1. **Откройте веб-панель:** `http://localhost:5173`
2. **Войдите как офицер:** `officer@probation.kg` / `123456`
3. **Перейдите в "Рабочие сессии"**
4. **Увидите сессию**, которую только что создал клиент
5. **Нажмите на сессию** для просмотра деталей
6. **Верифицируйте сессию** (или отклоните)
7. **После верификации** часы добавятся к completedHours клиента

---

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация сотрудника
- `POST /api/auth/login` - Вход (для сотрудников И клиентов)
- `GET /api/auth/me` - Получить текущего пользователя

### Клиенты
- `GET /api/clients` - Список клиентов
- `POST /api/clients` - Создать клиента
- `GET /api/clients/:id` - Получить клиента
- `GET /api/clients/:id/stats` - Статистика клиента
- `PUT /api/clients/:id` - Обновить клиента
- `DELETE /api/clients/:id` - Удалить клиента

### Рабочие сессии
- `POST /api/work-sessions/start` - Начать сессию
- `PUT /api/work-sessions/:id/end` - Завершить сессию
- `GET /api/work-sessions` - Список сессий
- `GET /api/work-sessions/:id` - Получить сессию
- `POST /api/work-sessions/:id/photos` - Загрузить фото
- `PUT /api/work-sessions/:id/verify` - Верифицировать сессию

---

## 🔧 Решение проблем

### Backend не запускается

```bash
# Проверьте что PostgreSQL запущен
# Windows: проверьте в Services
# Mac: brew services list
# Linux: sudo systemctl status postgresql

# Проверьте подключение к БД
psql -U postgres -d probation_db

# Проверьте настройки в backend/.env
```

### Мобильное приложение не подключается

1. **Убедитесь что backend запущен** и слушает на `0.0.0.0:5000`
2. **Проверьте IP адрес** в `mobile/src/api/axios.js`
3. **НЕ используйте** `localhost` или `127.0.0.1`!
4. **Телефон и компьютер** должны быть в одной Wi-Fi сети
5. **Отключите firewall** на компьютере (временно)

### Ошибка "Invalid credentials" при входе

- Проверьте что вы запустили `node seed.js` в backend
- Убедитесь что используете правильный email и пароль
- Для клиентов обязательно нужен `userType: 'client'` в запросе (уже исправлено)

### GPS не работает

- **В Expo Go:** разрешите доступ к местоположению
- **В эмуляторе:** отправьте фейковые координаты
- **На реальном устройстве:** включите GPS/Location Services

---

## 📁 Структура проекта

```
opr-raboty-/
├── backend/
│   ├── config/          # Конфигурация (БД)
│   ├── controllers/     # Контроллеры (бизнес-логика)
│   ├── middleware/      # Middleware (auth, validation, upload)
│   ├── models/          # Модели Sequelize
│   ├── routes/          # API routes
│   ├── utils/           # Утилиты
│   ├── .env            # Переменные окружения
│   ├── seed.js         # Скрипт создания тестовых данных
│   └── server.js       # Точка входа
├── frontend/
│   ├── src/
│   │   ├── api/        # Axios config
│   │   ├── components/ # React компоненты
│   │   ├── context/    # Context API
│   │   ├── pages/      # Страницы
│   │   └── main.jsx    # Точка входа
│   └── package.json
├── mobile/
│   ├── src/
│   │   ├── api/        # Axios config
│   │   ├── components/ # React Native компоненты
│   │   ├── navigation/ # Навигация
│   │   └── screens/    # Экраны
│   └── package.json
└── README.md
```

---

## 🔐 Безопасность

- Все пароли хешируются с помощью bcrypt (10 раундов)
- JWT токены для аутентификации
- CORS настроен для cross-origin запросов
- Helmet.js для security headers
- Rate limiting для API endpoints
- Валидация входных данных

---

## 🚀 Развертывание в production

### Backend

1. Измените `NODE_ENV=production` в .env
2. Установите сильный `JWT_SECRET`
3. Настройте реальную БД PostgreSQL
4. Настройте CORS только для нужных доменов
5. Используйте reverse proxy (nginx)
6. Настройте SSL/HTTPS

### Frontend

```bash
npm run build
# Разместите dist/ на веб-сервере
```

### Mobile

```bash
# Создайте production build
expo build:android
expo build:ios

# Или используйте EAS Build
eas build --platform android
eas build --platform ios
```

---

## 📝 Лицензия

Проект разработан для Департамента пробации Кыргызской Республики.

---

## 🤝 Поддержка

- **Email:** support@probation.kg
- **Телефон:** +996 XXX XXX XXX

---

## ✅ Чек-лист перед первым запуском

- [ ] PostgreSQL установлен и запущен
- [ ] Node.js 18+ установлен
- [ ] База данных `probation_db` создана
- [ ] Backend: `npm install` выполнен
- [ ] Backend: `.env` настроен
- [ ] Backend: `npm start` работает без ошибок
- [ ] Backend: `node seed.js` создал тестовые данные
- [ ] Frontend: `npm install` выполнен
- [ ] Frontend: `npm run dev` работает
- [ ] Mobile: `npm install` выполнен
- [ ] Mobile: IP адрес в `axios.js` изменен на ваш
- [ ] Mobile: Expo Go установлен на телефоне
- [ ] Mobile: `npm start` работает и показывает QR-код

---

**🎉 Готово! Система полностью настроена и готова к использованию!**

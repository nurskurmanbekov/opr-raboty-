# 🗺️ Установка интерактивной карты для геозон

## Что добавлено:

Интерактивная карта с OpenStreetMap для визуального выбора координат геозон!

### Возможности:
- 📍 Кликните по карте чтобы выбрать точку
- 🔵 Синий круг показывает радиус геозоны
- 🗺️ OpenStreetMap (бесплатная карта)
- ✨ Автоматическое обновление координат при клике
- 📏 Изменение радиуса обновляет круг на карте

---

## 🚀 Установка (ОБЯЗАТЕЛЬНО!):

### Шаг 1: Установить пакеты

```bash
cd frontend
npm install leaflet react-leaflet
```

### Шаг 2: Перезапустить frontend

```bash
npm run dev
```

Готово! Карта будет работать.

---

## 📦 Установленные пакеты:

- `leaflet` - библиотека для карт
- `react-leaflet` - React компоненты для Leaflet

---

## 🎯 Как использовать:

1. Войдите в систему как admin/superadmin
2. Перейдите в раздел "Геозоны"
3. Нажмите "Добавить геозону" или "Редактировать"
4. **Кликните по карте** чтобы выбрать точку
5. Координаты обновятся автоматически!
6. Измените радиус - круг изменится на карте
7. Сохраните геозону

---

## 🔧 Если карта не работает:

### Проблема: Карта не загружается

**Решение:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Проблема: Ошибка "Cannot find module 'leaflet'"

**Решение:**
```bash
cd frontend
npm install leaflet react-leaflet --save
```

### Проблема: Карта серая/пустая

**Причина:** Leaflet CSS не загрузился

**Решение:** Проверьте что в `frontend/index.html` есть:
```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
  integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
  crossorigin=""/>
```

---

## 📁 Добавленные файлы:

- `frontend/src/components/MapPicker.jsx` - компонент карты
- `frontend/src/pages/Geofences.jsx` - обновлен с картой
- `frontend/index.html` - добавлен Leaflet CSS

---

## 🎉 Готово!

Теперь создание геозон стало намного проще - просто кликни по карте! 🗺️✨

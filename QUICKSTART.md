# ⚡ Быстрый старт - 5 минут до запуска!

## 1️⃣ Backend

```bash
cd backend
npm install
createdb probation_db    # или создайте через psql
npm start                # Запустит сервер на :5000
npm run seed            # Создаст тестовые данные
```

## 2️⃣ Frontend

```bash
cd frontend
npm install
npm run dev             # Откроет http://localhost:5173
```

**Войдите:** `admin@probation.kg` / `123456`

## 3️⃣ Mobile

```bash
cd mobile
npm install

# ВАЖНО: Откройте mobile/src/api/axios.js
# Замените IP на свой (найдите через ipconfig/ifconfig)

npm start              # Запустит Expo
# Отсканируйте QR в Expo Go
```

**Войдите:** `client1@probation.kg` / `123456`

---

## ✅ Тест

1. В мобильном приложении: **Начать рабочую сессию**
2. Подождите 1 минуту
3. **Завершить сессию**
4. В веб-панели: перейдите в **"Рабочие сессии"**
5. **Верифицируйте** созданную сессию

🎉 **Готово!**

---

## 🆘 Проблемы?

- Backend не запускается → Проверьте PostgreSQL
- Mobile не подключается → Проверьте IP адрес в axios.js
- "Invalid credentials" → Запустите `npm run seed`

Полная документация: [README.md](./README.md)
